apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: cd-listener-push
spec:
  serviceAccountName: pipeline
  triggers:
    - name: github-push
      bindings:
        - ref: cd-binding
      template:
        ref: cd-template